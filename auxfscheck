# Check auxillary (non-root) filesystems.

type = scripted
command = /usr/bin/fsck -A -R -C -a>/dev/null; true
restart = false
options: starts-on-console start-interruptible skippable unmask-intr

depends-on: early-filesystems
depends-on: udevd
depends-on: rootrw
waits-for: udev-settle
